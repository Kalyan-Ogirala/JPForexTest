package com.kalyantest.draw.drawingapp.entity.Commands;

import lombok.Data;
import org.springframework.stereotype.Component;

@Data
@Component
public class Q implements Command {
    @Override
    public void executeCommand() {
        System.exit(0);
    }
}
